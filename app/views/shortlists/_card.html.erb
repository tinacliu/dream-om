<div class= "d-flex container mr-0 p-0 text-box">
  <div class="card card-index card-comment shadow-sm">
    <div class="plot-note">
      <div class="row ">
        <%= link_to shortlist_path(shortlist), method: :delete, data: { confirm: "Are you sure?" } do %>
          <i class="fas fa-times-circle xl_font f-shadow text-light ml-1"></i>
        <% end %>
      </div>
    </div>
    <div id="collapseOne-<%= shortlist.id %>" class="collapse show" aria-labelledby="headingOne" data-parent="#accordionExample">
        <% if shortlist.plot.image_url.blank? == false %>
          <%= image_tag shortlist.plot.image_url, crop: :fill, class: 'card-index-img' %>
        <% end %>
      <%= link_to plot_path(shortlist.plot) do%>
        <div class="card-body bg-light d-flex justify-content-between align-items-center py-3">
          <div class="card-left">
            <h6 class="m-0 grey" id="index-card-title">
                <%= truncate shortlist.plot.street_name, length: 20, separator: /\s/, omission: '' %>
            </h6>
            <p class="mb-0 grey h_font s_font">
              <% post_town = truncate shortlist.plot.post_town, length: 10, separator: /\s/, omission: '' %>
                <%= "#{post_town} #{shortlist.plot.outcode}" %>
            </p>
          </div>
          <div class="card-right text-right">
            <h6 class="card-subtitle blue bold"><%= shortlist.plot.property_type %></h6>
            <% a = shortlist.plot.price.to_s.size - 3 %>
            <p class="mb-0 grey bold h_font s_font">
              <% if a >= 4 %>
                <%= "£ #{shortlist.plot.price.to_s[0]},#{shortlist.plot.price.to_s[1..3]},#{shortlist.plot.price.to_s[4..7]}" %>
              <% else %>
                <%= "£ #{shortlist.plot.price.to_s[0...a]},#{shortlist.plot.price.to_s[a..-1]}" %>
              <% end %>
            </p>
          </div>
        </div>
      <% end %>
    </div>
    <div class="">
    <div class="accordion" id="accordionExample">
      <div class="comment-container">
        <div class="comment-card" id="headingOne">
          <h2 class="mb-0">
            <button class="btn btn-link text-light btn-none" type="button" data-toggle="collapse" data-target="#collapseOne-<%= shortlist.id %>" aria-expanded="false" aria-controls="collapseOne">
              <!-- you can click anywhere to expand -->
              <i class="fas fa-comment-alt white l_font pb-3"></i>
            </button>
          </h2>
        </div>

        <div id="collapseOne-<%= shortlist.id %>" class="collapse" aria-labelledby="headingOne" data-parent="#accordionExample">
          <%= simple_form_for [shortlist, comment] do |f| %>
          <% if shortlist.comment.nil? %>
            <%= f.input :content, label: "", input_html: {value: ''}, placeholder: "Make a note about the plot" %>
          <% else %>
            <%= f.input :content, label: "", input_html: {value: shortlist.comment.content }, placeholder: "Make a note about the plot.." %>
          <% end %>
          <%= f.submit "Record Note", class: "btn btn-primary ml-4 mb-4" %>
          <% end %>
        </div>
      </div>
    </div>
  </div>
 </div>

  <div class="justify-content-center plot-profile">
  <!-- <div class="d-flex justify-content-center"> -->
    <div class="row grey justify-content-center">
      <h6 class="card-title thin mb-0 pt-2">Plot size</h6>
    </div>
    <div class="row blue justify-content-center mb-4 pb-1">
      <% if shortlist.plot.plot_size.blank?%>
        <h6>--</h6>
      <% else %>
        <%= shortlist.plot.plot_size%>m&sup2;
      <% end %>
    </div>
    <div class="border-bottom"></div>
    <div class="row grey justify-content-center mt-3 pt-2">
      <h6 class= "card-title m-0 thin" >Floor size</h6>
    </div>
    <div class="row blue justify-content-center mb-3 pb-3">
      <% if shortlist.plot.floor_size.blank?%>
        <h6>--</h6>
      <% elsif shortlist.plot.floor_size%>
        <h6><%= shortlist.plot.floor_size.split(',').first%> sq.ft</h6>
      <% end %>
    </div>
    <div class="border-bottom"></div>
    <div class="row grey justify-content-center thin mt-3 pb-2">
        <!-- <h6 class= "card-title thin m-0" ></h6> -->
      <% if shortlist.plot.plan_granted == true %>
        <h6 class= "card-title thin m-0">Planning<br>Granted</h6>
      <% else %>
      <h6><br></h6>

      <% end %>
    </div>
  </div>
</div>













